<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginEnd="20dp"
    android:gravity="center_horizontal"
    android:orientation="vertical">

    <FrameLayout
        android:id="@+id/view_active_ring"
        android:layout_width="72dp"
        android:layout_height="72dp"
        android:background="@drawable/bg_active_avatar_ring_neutral"
        android:padding="4dp">

        <ImageView
            android:id="@+id/iv_active_avatar"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/avatar_circle_bg"
            android:clipToOutline="true"
            android:contentDescription="@string/cd_profile_photo"
            android:scaleType="centerCrop"
            tools:src="@drawable/profile_placeholder_bg" />

        <View
            android:id="@+id/view_online_dot"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:layout_gravity="bottom|end"
            android:background="@drawable/bg_active_online_dot"
            tools:visibility="visible" />

    </FrameLayout>

    <TextView
        android:id="@+id/tv_active_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="@color/login_text_secondary"
        android:textSize="12sp"
        tools:text="Sarah" />

</LinearLayout>
