<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/card_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardBackgroundColor="@color/notification_card_surface"
    app:cardCornerRadius="12dp"
    app:cardElevation="6dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:minHeight="80dp"
        android:orientation="horizontal"
        android:paddingStart="16dp"
        android:paddingTop="16dp"
        android:paddingEnd="16dp"
        android:paddingBottom="16dp">

        <FrameLayout
            android:id="@+id/avatar_container"
            android:layout_width="48dp"
            android:layout_height="48dp">

            <com.google.android.material.imageview.ShapeableImageView
                android:id="@+id/iv_avatar"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                android:src="@drawable/profile_placeholder_bg"
                app:shapeAppearanceOverlay="@style/ShapeAppearance.MaterialComponents.SmallComponent" />

            <FrameLayout
                android:id="@+id/small_chip"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_gravity="bottom|end"
                android:layout_marginEnd="-4dp"
                android:layout_marginBottom="-4dp"
                android:background="@drawable/notification_small_chip_bg"
                android:visibility="gone">

                <ImageView
                    android:id="@+id/iv_small_icon"
                    android:layout_width="12dp"
                    android:layout_height="12dp"
                    android:layout_gravity="center"
                    android:tint="@color/notification_chip_icon"
                    app:srcCompat="@drawable/ic_heart" />

            </FrameLayout>
        </FrameLayout>

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="12dp"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tv_message"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:maxLines="2"
                android:textColor="@color/notification_primary_text"
                android:textSize="14sp"
                android:fontFamily="sans-serif-medium" />

            <TextView
                android:id="@+id/tv_time"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:textColor="@color/notification_meta_text"
                android:textSize="12sp" />

        </LinearLayout>

        <FrameLayout
            android:id="@+id/trailing_container"
            android:layout_width="wrap_content"
            android:layout_height="48dp"
            android:foregroundGravity="center">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_follow"
                android:layout_width="wrap_content"
                android:layout_height="32dp"
                android:background="@drawable/search_follow_button_bg"
                android:insetTop="0dp"
                android:insetBottom="0dp"
                android:minWidth="76dp"
                android:paddingStart="16dp"
                android:paddingEnd="16dp"
                android:text="@string/profile_action_follow"
                android:textAllCaps="false"
                android:textColor="@color/theme_on_primary"
                android:textSize="12sp"
                android:textStyle="bold"
                android:visibility="gone"
                app:backgroundTint="@null"
                app:cornerRadius="16dp"
                app:iconPadding="0dp"
                app:rippleColor="@android:color/transparent"
                app:strokeWidth="0dp" />

            <com.google.android.material.card.MaterialCardView
                android:id="@+id/card_preview"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:visibility="gone"
                app:cardBackgroundColor="@android:color/transparent"
                app:cardCornerRadius="8dp"
                app:cardElevation="0dp">

                <ImageView
                    android:id="@+id/iv_preview"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:contentDescription="@string/cd_post_image"
                    android:scaleType="centerCrop" />

            </com.google.android.material.card.MaterialCardView>

            <FrameLayout
                android:id="@+id/placeholder_preview"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="@drawable/notification_media_placeholder_bg"
                android:visibility="gone">

                <ImageView
                    android:id="@+id/iv_placeholder_icon"
                    android:layout_width="18dp"
                    android:layout_height="18dp"
                    android:layout_gravity="center"
                    android:tint="@color/notification_placeholder_icon"
                    app:srcCompat="@drawable/ic_story_sparkles" />

            </FrameLayout>

        </FrameLayout>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
